node_modules/
dist/
.npm-cache/
.electron-cache/
.electron-builder-cache/
logs/
*.log
.tmp-test/
issue28/
.tmp-*.png
src-tauri/target/
# 无头 Edge/CDP 验证截图（scratch，可重新生成）
.shot/
# DSH 运行时产物默认不入库；但 .dsh/skills 是随仓库维护的规则/经验存档，必须入库。
.dsh/*
!.dsh/skills/
# npm 误跑 npm install 会生成空壳锁文件；本仓库前端走 tauri / npx，不用 npm
package-lock.json
